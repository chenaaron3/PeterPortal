components:
  parameters:
    ucinetidParam:
      in: path
      name: ucinetid
      required: true
      schema:
        $ref: '#/components/schemas/ProfessorID'
    courseIDParam:
      in: path
      name: courseID
      required: true
      schema:
        $ref: '#/components/schemas/CourseID'